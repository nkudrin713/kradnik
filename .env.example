POSTGRES_PASSWORD=change-me

# Local development defaults. Production can use POSTGRES_HOST=postgres
# when the application runs in the same Docker Compose network as the database.
POSTGRES_HOST=localhost
POSTGRES_PORT=5433
POSTGRES_DB=kradnik
POSTGRES_USER=kradnik
POSTGRES_PUBLIC_PORT=5433

TELEGRAM_BOT_TOKEN=
